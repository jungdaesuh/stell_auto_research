[project]
name = "autoresearch"
version = "0.1.0"
description = "Autonomous AI research harness for stellarator coil optimization"
readme = "README.md"
requires-python = ">=3.10"
# The harness itself (run.py, tests) is stdlib-only. Solver dependencies
# (simsopt etc.) live in the interpreter pointed to by SIMSOPT_PYTHON,
# not in this project's environment.
dependencies = []
